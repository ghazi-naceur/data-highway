spark {
  app-name = handler
  master-url = "local[*]"
  log-level = warn
}

elasticsearch {
  es-nodes = "http://localhost:9200"
}

hadoop {
  host = "hdfs://localhost:9000"
}

cassandra {
  host = "localhost"
  port = "9042"
}