spark {
  app-name = handler
  master-url = "local[*]"
  log-level = {
    type = warn
  }
}

elasticsearch {
  host = "http://localhost"
  port = "9200"
}

hadoop {
  host = "hdfs://localhost"
  port = "9000"
}

cassandra {
  host = "localhost"
  port = "9042"
}

postgres {
  host = "jdbc:postgresql://localhost"
  port = "5432"
  user = "admin"
  password = "admin"
}

app {
  tmp-work-dir = "/tmp/data-highway-playground"
}